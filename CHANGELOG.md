# Changelog

本文件记录 NeoArchive 的重要用户可见变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

### 新增

- **Adobe 风格主页（启动界面）**：程序打开时先进入主页，可选择上次保存的项目或历史项目。
  - 左上角「上次打开」大卡（2×2），其余为统一尺寸的 16:9「最近项目」卡片，默认一行 4 张，窗口缩放/最大化时卡片等比例放大。
  - 卡片封面取工程第一张背景图（本地导入素材），比例与编辑器画面一致；无图时显示首字母占位图。
  - 主页支持自定义背景图（左下角可更换/恢复默认），图片自动压缩并持久化。
  - 侧边栏为毛玻璃效果（半透明白色高光 + 模糊），背景配暗色蒙版与文字投影保证可读性。
  - 主页始终使用深色分层风格，不受编辑器日间/夜间主题影响。
- **「主页」按钮**：编辑器顶栏可返回主页，随时切换工程。
- **打开工程的过渡动画**：点击项目卡片 → 3D 翻转 → 白色过渡层从卡片中心展开盖满全屏 → 进入编辑器。
- **Esc 退出全屏**：主页与编辑页一致，按 Esc 可退出全屏。

### 修复

- 项目卡片封面首次打开加载不出图片：改用 `resolveBackground` 统一解析（内置/本地导入/URL），并订阅本地素材（IndexedDB）异步加载完成后刷新封面。
- 窗口最大化时卡片不放大：改为固定列数 + `1fr`，卡片随窗口等比例缩放。
